const Carousel = () => {
  return (
    <div
      className="carousel"
      style={{
        width: '100%',
        height: '320px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'green',
      }}
    >
      carousel(layout 예시)
    </div>
  );
};
export default Carousel;
