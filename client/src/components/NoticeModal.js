const NoticeModal = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      알림모달
    </div>
  );
};
export default NoticeModal;
