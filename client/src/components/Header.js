const Header = () => {
  return (
    <header
      style={{
        width: '100%',
        height: '85px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'blue',
      }}
    >
      헤더(layout 예시)
    </header>
  );
};
export default Header;
